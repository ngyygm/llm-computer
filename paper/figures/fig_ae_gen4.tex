\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.88\textwidth]{figures/generated/fig_ae_gen4}
  \caption{Generation~IV agent frameworks versus modern operating systems (Linux/Windows~NT).
  Codex's sub-agent spawning maps to \texttt{fork()};
  its sandbox maps to \texttt{chroot}/container isolation;
  Claude Code's hooks parallel Linux Security Module (LSM) hooks;
  and the approval gate corresponds to \texttt{sudo} privilege elevation.
  The key advance: confronting real-world codebases with irreversible side effects
  demands formal isolation and human-in-the-loop approval.}
  \label{fig:ae:gen4}
\end{figure}
