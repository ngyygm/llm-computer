\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.80\textwidth]{figures/generated/fig_bg_22_model}
  \caption{Model layer: the Transformer's self-attention mechanism serves as the general-purpose inference core; Mixture-of-Experts (MoE) introduces sparse routing so that only a subset of parameters is activated per inference step, enabling capacity to scale without proportional compute cost.}
  \label{fig:bg22:model}
\end{figure}
